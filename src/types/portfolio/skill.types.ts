// ========================================
// Skills 관련 타입 정의
// ========================================

export interface SkillGroup {
  category: string;
  label: string;
  items: string[]; // 뱃지 이름 배열 (예: ['react', 'typescript'])
}

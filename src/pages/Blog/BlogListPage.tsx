import styles from './BlogListPage.module.css';

const BlogListPage = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>Blog</h1>
      <p className={styles.description}>블로그 목록이 여기에 들어갑니다.</p>
    </div>
  );
};

export default BlogListPage;
